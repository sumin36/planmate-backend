spring:
  datasource:
    url: jdbc:mysql://localhost:3306/planmate?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: planmate-secret-key-12345678901234567890
  access-expiration: 3600000 # 1시간
  refresh-expiration: 604800000 # 7일
